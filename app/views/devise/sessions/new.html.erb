<div class="row justify-content-center">
  <div class="col-md-7 col-lg-5">
    <div class="wrap">
      <div class="login-wrap p-4 p-md-1">
        <br>
        <h2 class="tc tw">Log in</h2>
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="form-floating">
            <%= f.email_field :email, class: "form-control" %>
            <%= f.label :email, "Email address", class: "form-control-placeholder" %>
          </div>
          <div class="form-floating">
            <%= f.password_field :password, class: "form-control" %>
            <%= f.label :password, "Password", class: "form-control-placeholder" %><br />
          </div>

          <% if devise_mapping.rememberable? %>
            <div class="form-group">
              <%= f.check_box :remember_me %>
              <%= f.label :remember_me, class: "form-control-placeholder tw" %>
            </div>
          <% end %>
          <div class="form-group">
            <%= f.submit "Log in", class: "form-control btn btn-outline-light rounded submit px-3" %>
          </div>
        <% end %>
        <br><br>
      </div>
    </div>
  </div>
</div>
